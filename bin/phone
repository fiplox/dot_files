#!/bin/sh
#su - user -c 'export DISPLAY=:0; /home/user/bin/scrcpy -b 2M -m 800'
#export DISPLAY=:0.0
#sudo adb kill-server
#sudo adb start-server
export DISPLAY=:0 
export ADB=$(which adb)
sleep 0.3
# adb kill-server
#sleep 1
# adb start-server
#sleep 1
#/home/user/bin/scrcpy -b2M --max-size 800 |& tee -a /tmp/phone.log
/home/user/bin/scrcpy -b2M |& tee -a /tmp/phone.log
