#!/bin/sh
#su - user -c 'export DISPLAY=:0; /usr/bin/scrcpy -b 2M -m 800'
#sudo adb kill-server
#sudo adb start-server
export DISPLAY=:0 
#export ADB=/usr/bin/adb
#sleep 0.3
#su user -c /usr/bin/scrcpy |& tee -a /tmp/phone.log
# adb kill-server
#sleep 1
# adb start-server
#sleep 1
#/home/user/bin/scrcpy -b2M --max-size 800 |& tee -a /tmp/phone.log
/usr/bin/scrcpy -b2M |& tee -a /tmp/phone.log
